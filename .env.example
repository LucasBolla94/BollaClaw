# BollaWatch Configuration
PORT=21087
HOST=0.0.0.0
DB_PATH=./data/bollawatch.db
LOG_LEVEL=info
# Max events to keep (auto-cleanup older ones)
MAX_EVENTS=100000
# Max age in days for auto-cleanup
MAX_AGE_DAYS=30
